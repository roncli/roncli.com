<div class="col-xs-8">
    <div class="panel panel-primary">
        <div class="panel-heading text-center"><h3>ronc.li Redirects</h3></div>
        <div class="panel-body">
            Use this list to add and remove redirects to ronc.li.<br /><br />
            {{#each redirects.models}}
                <div class="navbar navbar-default redirect" data-redirect-id="{{attributes.id}}" style="background-color: #eeeeee; border: solid 1px #cccccc;">
                    <div class="container-fluid">
                        <div class="navbar-left" style="margin-right: 20px;">
                            <ul class="nav navbar-nav">
                                <li class="navbar-text" style="font-size: 20px; padding: 10px 0; color: #000000;">{{attributes.fromPath}}</li>
                            </ul>
                        </div>
                        <div class="navbar-right" style="margin-right: 0;">
                            <ul class="nav">
                                <li>
                                    <div>
                                        <a href="{{attributes.toUrl}}" class="btn btn-primary navbar-btn new-window" target="_blank"><span class="glyphicon glyphicon-globe"></span> {{attributes.toUrl}}</a>
                                        <button class="btn btn-danger navbar-btn remove-redirect"><span class="glyphicon glyphicon-trash"></span> Remove</button>
                                    </div>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            {{/each}}
            {{#ifCond redirects.models.length "===" 0}}
                There are no ronc.li redirects.
            {{/ifCond}}
        </div>
        <div class="panel-body">
            <div class="input-group">
                <span class="input-group-addon">From Path:</span>
                <input id="redirect-from-path" type="text" maxlength="255" class="form-control" placeholder="/your/url/here"/>
            </div>
            <div class="input-group">
                <span class="input-group-addon">To URL:</span>
                <input id="redirect-to-url" type="text" maxlength="1024" class="form-control" placeholder="/your/url/here"/>
            </div>
            <div class="text-center">
                <button class="btn btn-primary" id="add-redirect"><span class="glyphicon glyphicon-plus"></span> Add Redirect</button>
            </div>
        </div>
    </div>
</div>
<div class="col-sm-2 hidden-xs right-side">
    <div class="panel panel-primary">
        <div class="panel-heading panel-heading-small text-center">Site Administration</div>
        <ol class="breadcrumb">
            <li><a href="/">Home</a></li>
            <li><a href="/admin">Admin</a></li>
            <li class="active">Redirects</li>
        </ol>
        <div class="panel-body">
            Add or remove ronc.li redirects.<br/><br/>
            Please keep in mind that you cannot navigate directly to the URL of this page, nor may you bookmark this page. You must access this page via the Admin menu.
        </div>
    </div>
</div>
